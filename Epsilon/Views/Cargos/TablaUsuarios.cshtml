@* @using Presentacion.ViewModels

@model IEnumerable<ViewDatosUsuario>

<table class="table">
    <thead>
        <tr>
            <th scope="col" class="text-center" width="1%">#</th>
            <th scope="col">NIF</th>
            <th scope="col">Nombre</th>
            <th scope="col">Área de trabajo</th>
            <th scope="col">Habilitado</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>
    <tbody id="idBodyTable">
    @if (Model.Count() > 0)
    {
        foreach (var dusu in Model)
        {
            <tr id="idFilaUsuario_@dusu.IdUsuario">
                @{
                    await Html.RenderPartialAsync("FilaUsuario", dusu);
                }
            </tr>
        }
    }else {
        <tr>
            <td colspan="5" class="text-center">
                <span class="text-black-50 m-2">(No hay resultados que mostrar)</span>
            </td>
        </tr>
    }
    </tbody>
</table> *@